<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Include Spring Boot's default configuration -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    
    <!-- Property files -->
    <property name="LOGS" value="./logs"/>
    
    <!-- Console Appender -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>
    
    <!-- File Appender -->
    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/spring-boot-logger.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/archived/spring-boot-logger-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>
    
    <!-- Log Levels -->
    <logger name="com.crowdaid" level="info" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </logger>
    
    <!-- Hibernate Logging -->
    <logger name="org.hibernate.SQL" level="debug" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </logger>
    
    <logger name="org.hibernate.type.descriptor.sql" level="trace" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </logger>
    
    <!-- Spring Security Logging -->
    <logger name="org.springframework.security" level="info" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </logger>
    
    <!-- WebSocket Logging -->
    <logger name="org.springframework.web.socket" level="info" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </logger>
    
    <!-- Root Logger -->
    <root level="info">
        <appender-ref ref="Console"/>
        <appender-ref ref="File"/>
    </root>
    
    <!-- Profile-specific logging -->
    <springProfile name="dev">
        <logger name="com.crowdaid" level="debug"/>
        <logger name="org.springframework.web" level="debug"/>
        <logger name="org.hibernate" level="debug"/>
    </springProfile>
    
    <springProfile name="prod">
        <logger name="com.crowdaid" level="info"/>
        <root level="warn">
            <appender-ref ref="Console"/>
            <appender-ref ref="File"/>
        </root>
    </springProfile>
    
    <springProfile name="test">
        <logger name="com.crowdaid" level="warn"/>
        <root level="warn">
            <appender-ref ref="Console"/>
        </root>
    </springProfile>
</configuration>
